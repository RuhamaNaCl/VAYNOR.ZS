function renderWishlist(){let e=$("#wishlist-grid");if(!e)return;let a=VAYNOR_PRODUCTS.filter(p=>getWish().includes(p.id));e.innerHTML=a.length?a.map(productCard).join(''):'<div class="empty card" style="grid-column:1/-1"><h2>Your wishlist is empty</h2><p>Save products you want to revisit.</p><a class="btn" href="shop.html">Explore products</a></div>'}document.addEventListener("DOMContentLoaded",renderWishlist)