document.addEventListener("DOMContentLoaded",()=>{const g=$("#product-grid");if(!g)return;const q=new URLSearchParams(location.search);let cat=q.get("category")||"";const s=$("#shop-search"),sort=$("#sort");function run(){let a=[...VAYNOR_PRODUCTS],term=(s?.value||q.get("q")||"").toLowerCase();if(term)a=a.filter(p=>(p.name+" "+p.category+" "+p.desc+" "+p.tag).toLowerCase().includes(term));if(cat)a=a.filter(p=>p.category===cat);if(sort?.value==="low")a.sort((x,y)=>x.price-y.price);if(sort?.value==="high")a.sort((x,y)=>y.price-x.price);if(sort?.value==="new")a.reverse();renderProducts(a);$("#result-count")&&($("#result-count").textContent=a.length+" products")}s?.addEventListener("input",run);sort?.addEventListener("change",run);$$(".category-filter").forEach(b=>b.addEventListener("click",()=>{cat=b.dataset.category;$$(".category-filter").forEach(x=>x.classList.remove("active"));b.classList.add("active");run()}));run()})